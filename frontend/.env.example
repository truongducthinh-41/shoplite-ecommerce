# Create a .env file locally and set this to your backend API URL if not using localhost.
# On Vercel, add this environment variable in the project settings.
# VITE_API_URL=https://your-backend-api-url.onrender.com/api
